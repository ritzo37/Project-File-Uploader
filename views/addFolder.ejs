<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Folder</title>
    <style>
      body {
        background-color: #2e2e2e;
        color: #f5f5f5;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      form {
        background-color: #3a3a3a;
        padding: 2rem;
        border-radius: 10px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        width: 300px;
      }

      label {
        display: block;
        margin-bottom: 1rem;
        font-size: 1rem;
      }

      input[type="text"] {
        width: 100%;
        padding: 0.5rem;
        margin-top: 0.5rem;
        border: none;
        border-radius: 5px;
        background-color: #555;
        color: white;
      }

      button {
        width: 100%;
        padding: 0.75rem;
        border: none;
        border-radius: 5px;
        background-color: #1e90ff;
        color: white;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }

      button:hover {
        background-color: #1c7cd6;
      }
    </style>
  </head>
  <body>
    <form action="/<%= userId %>/<%= folderId %>/addFolder" method="post">
      <label for="name">
        Folder Name
        <input type="text" name="folderName" id="name" required />
      </label>
      <button type="submit">Add Folder</button>
    </form>
  </body>
</html>
